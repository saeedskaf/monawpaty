# Template for Android build-time secrets.
# Copy this file to `secrets.properties` (same directory) and fill in real values.
# `secrets.properties` is gitignored and must never be committed.
MAPS_API_KEY=YOUR_ANDROID_GOOGLE_MAPS_API_KEY
