/// Template for local app configuration.
///
/// Copy this file to `app_config.dart` (same directory) and fill in the real
/// values. `app_config.dart` is gitignored and must never be committed.
class AppConfig {
  /// Base URL of the backend API, e.g. "https://api.example.com/polls".
  static const String baseUrl = "https://your-backend.example.com/polls";
}
